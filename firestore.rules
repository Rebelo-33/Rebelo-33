// ✅ firestore.rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read/write access to lists collection if the correct PIN is used
    match /lists/{listId} {
      allow read, write: if true;

      // Future improvement: restrict write to original creator using Firestore Auth
    }

    // Add more rule sets as needed
  }
}
